export function calculate(){

    const txtArea = document.getElementById("code-txt-area");
    const content = txtArea.value.trim();

    if(!content)
        alert("Please insert some code to calculate")

    
}